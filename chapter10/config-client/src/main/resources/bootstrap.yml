spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
  profiles:
    active: dev   #这个是请求http://localhost:8769 这个地址下的Spring cloud配置文件的下的dev结束的配置文件，这个要用bootstrap的，bootstrap优先于application

